// const formatDateInput = text => {
//   // Remove non-digit characters
//   const cleaned = text.replace(/\D/g, '');

//   // Add dashes after YYYY and MM
//   if (cleaned.length <= 4) return cleaned; // YYYY
//   if (cleaned.length <= 6) return `${cleaned.slice(0, 4)}-${cleaned.slice(4)}`; // YYYY-MM
//   return `${cleaned.slice(0, 4)}-${cleaned.slice(4, 6)}-${cleaned.slice(6, 8)}`; // YYYY-MM-DD
// };
